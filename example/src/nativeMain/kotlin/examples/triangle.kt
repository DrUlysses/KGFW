package examples

expect fun triangle(
    startTime: Long,
    mouseX: Int,
    mouseY: Int,
    windowWidth: Int,
    windowHeight: Int,
    framebufferWidth: Int = windowWidth,
    framebufferHeight: Int = windowHeight
)
